// Запрос числа  и перевод его из строки в число
/*Console.WriteLine("Введите число А");
int a = int.Parse(Console.ReadLine());*/

// квадратный корень
// double a = Math.Sqrt ();

// Возведение в степень, первое число - основание, второе число в скобках - степень
// Math.Pow (a, 2);

// метод создания массива

/*void FillArray(int[] collection)
{
    int length = collection.Length;
    int index = 0;
    while(index<length)
    {
        collection[index] = new Random().Next(0,100);
        index++;
    }
}
int [] array = new int[];*/
// простой способ задания массива, тут из двух элементов
/*int [] arr = new int[2];
arr[0] = 1;
arr[1] = 2;*/

// генератор случайных чисел от ноля до 10 (второе число в скобках -1)

// double a = new Random().Next (1,11);